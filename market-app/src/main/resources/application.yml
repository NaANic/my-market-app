spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/shopfront
    username: shopfront
    password: shopfront

  sql:
    init:
      mode: always

  data:
    redis:
      host: localhost
      port: 6379

  thymeleaf:
    cache: false

  profiles:
    include: oauth2

cache:
  item:
    ttl-minutes: 5

payment:
  service:
    url: http://localhost:8081
